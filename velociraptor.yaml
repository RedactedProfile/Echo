scripts:
  start: 
    cmd: .\echo.ts 
    # log: debug 
    lock: lock.json 
    allow: 
      - net 
      - env 
  start-fresh: deno run --allow-net --allow-env --lock=lock.json --lock-write --reload .\echo.ts